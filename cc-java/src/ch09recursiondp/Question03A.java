package ch09recursiondp;

/**
 * 9.3 배열 A[0...n-1]에서 A[i] = i 인 인덱스 i를 Magic Index라 정의하자.
 *     정렬된 상태의 배열이 주어졌을 때, 마술 인덱스가 존재한다면 그 값을 찾는 메서드를 작성하라.
 *     배열 안에 중복된 값은 없다.
 *
 *     변형: 배열에 저장되는 값에 중복이 있다면 어떻게 풀 수 있겠는가?
 */
public class Question03A {
    public static int magicSlow(int[] array) {
        return -1;
    }

    public static int magicFast(int[] array, int start, int end) {
        return -1;
    }

    public static int magicFast2(int[] array, int start, int end) {
        return -1;
    }
}
