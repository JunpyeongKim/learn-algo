package ch10sortsearch;

/**
 * 10.8 정수 스트림을 읽는다고 하자. 주기적으로 어떤 수 x의 랭킹(x보다 같거나 작은 수의 개수)을 확인하고 싶다.
 *      해당 연산을 지원하는 자료구조와 알고리즘을 구현하라.
 *      수 하나를 읽을 때마다 호출되는 메서드 track(int x)와,
 *      x 보다 같거나 작은 수의 개수(x 자신은 포함하지 않는다)를 반환하는 메서드 getRankOfNumber(int x)를 구현하면 된다.
 *
 *      - 예
 *          입력 스트림: 5, 1, 4, 4, 5, 9, 7, 13, 3
 *          getRankOfNumber(1) = 0
 *          getRankOfNumber(3) = 1
 *          getRankOfNumber(4) = 3
 *
 * (6E)
 */
public class SortingSearching08TBD {
}
