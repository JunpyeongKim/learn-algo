package ch04treegraph;

/**
 * 4.9 각 노드에 어떤 값이 저장되어 있는 이진 트리 하나와 값 n이 주어졌을 때,
 *     n과 같은 값을 갖는 모든 경로를 찾아라.
 *     어떤 경로의 값은 그 경로에 포함된 모든 노드의 값의 합이며,
 *     경로는 트리 내의 아무 위치에서나 시작하고 끝날 수 있다.
 *
 * (4E)
 *
 * (6E)
 * 4.12 Path with Sum: You are given a binary tree in which each node contains an integer value
 *                     (which might be positive or negative).
 *                     Design an algorithm to count the number of paths that sum to a given value.
 *                     The path does not need to start or end at the root or a leaf,
 *                     but it must go downwards (traveling only from parent nodes to child nodes).
 */
public class TreeGraph09TBD {
}
